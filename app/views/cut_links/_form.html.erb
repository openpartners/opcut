<%= form_for(@cut_link) do |f| %>
  <% if @cut_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cut_link.errors.count, "error") %> prohibited this cut_link from being saved:</h2>

      <ul>
      <% @cut_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :cut %><br />
    <%= f.text_field :cut, :value => ('a'..'z').to_a.shuffle[0..7].join %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
